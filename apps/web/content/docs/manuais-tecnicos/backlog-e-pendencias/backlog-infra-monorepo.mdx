---
title: Backlog de Infraestrutura e Estabilizacao
description: Backlog priorizado com os itens pendentes para estabilizacao do app, consolidacao arquitetural e evolucao das novas funcionalidades.
lastUpdated: 2026-03-28
owner: Engenharia de Plataforma
status: published
tags:
  - manuais-tecnicos
  - backlog-e-pendencias
  - backlog-infra-monorepo
audience:
  - admin
  - developer
  - suporte
---

## Objetivo

Concentrar apenas o backlog ainda pendente, separando por modulo:

- backlog de implementacao
- ideias de implementacao

Este documento nao substitui a documentacao de arquitetura, estrategia do modulo remoto ou manuais operacionais. Ele consolida apenas o que ainda falta executar ou amadurecer.

## Diretriz de execucao

Ordem recomendada:

1. estabilizar o app web atual
2. reforcar tipagem, tema e consistencia visual
3. consolidar CI, observabilidade e contratos
4. fechar o MVP remoto fim a fim
5. consolidar onboarding remoto via NSIS + Registry
6. conectar shells de multi-app a casos de uso reais

## Modulos

### Infraestrutura, contratos e qualidade

#### Backlog de implementacao

- [ ] `INFRA-026` [Media] Metricas minimas por rota critica, jobs e integracoes externas.
- [ ] `INFRA-027` [Alta] Testes E2E para fluxos criticos do app e docs. A base de Playwright ja foi aberta em `apps/web`, com config, scripts, smoke de landing page, redirect de rotas protegidas (`/portal` e `/docs`) e fluxo autenticado opcional por credenciais de ambiente; faltam integrar execucao ao CI, instalar browsers no pipeline e ampliar a cobertura para fluxos autenticados e operacionais mais profundos.
- [x] `INFRA-027A` [Media] Ligar `apps/api` aos casos de uso reais do `packages/api`. Status atual: **concluido**. Fechamento deste recorte: `remoteRouter` conectado ao `appRouter`; contrato remoto parseado por schema; erro tipado preservado (`code/message/httpStatus`) no shell HTTP; `discover/bootstrap/sync/ack` ligados ao `remote-domain` com `requestIp` e logger reais; `sessions/hosts/address-book` sem blocos `not-wired`; reducao forte de duplicacao com wrappers do `apps/web` consumindo ports centrais do `packages/api`; suite minima do `remoteRouter` criada em `packages/api/tests/remote-router.test.ts`; e validacao local executada com sucesso em `@dosc-syspro/api` (`typecheck` + `test`).
- [ ] `INFRA-027B` [Media] Materializar runtime real de `apps/mobile` com boundaries ja definidos.
- [ ] `INFRA-027C` [Media] Revisar o que ainda deve sair de `apps/web/src/lib` para consumo compartilhado via `packages/*`.

#### Ideias de implementacao

- Adicionar observabilidade transversal com metricas de duracao de sessao remota e tempo de resposta do Zammad no snapshot de observabilidade existente em `/api/platform/observability/zammad`.
- Revisar acessibilidade transversal de filtros e paineis operacionais, incluindo `aria-label`, `role="main"` e estados de loading por rota.
- Remover hardcode de locale em `formatRecency` e permitir `locale` configuravel para futura internacionalizacao.

### Docs e experiencia publica

#### Backlog de implementacao

- [ ] `INFRA-028` [Media] Historico de buscas no modal.
- [ ] `INFRA-029` [Media] Filtro de busca por categoria/tag.
- [ ] `INFRA-030` [Media] `sitemap.xml` automatico para docs.
- [ ] `INFRA-031` [Media] Analytics de paginas e buscas sem resultado.
- [ ] `INFRA-032` [Media] Copy button com feedback animado.
- [ ] `INFRA-033` [Media] Feedback "Esse conteudo ajudou?" com persistencia.
- [ ] `INFRA-034` [Media] Acessibilidade WCAG AA na rota `/docs`.
- [ ] `FEAT-006` [Baixa] Evoluir area publica para aderir integralmente aos tokens de tema e remover hardcodes restantes.

#### Ideias de implementacao

- Criar locking de base de conhecimento tecnica para separar manuais internos e manuais do usuario final.
- Avaliar melhorias PWA e notificacoes push para consumo de docs e portal em campo.

### Plataforma remota e RustDesk

#### Backlog de implementacao

- [ ] `FEAT-001` [Alta] Materializar MVP de plataforma remota com entidades-base, persistencia e rotas iniciais. Persistencia, tenant enforcement, rotas de `hosts`, `sessions`, `discover`, `bootstrap`, `sync` e `ack`, separacao entre configuracao e operacao, detalhe do host, CRUD basico de host com `RustDesk ID`, busca operacional do modulo, rate limit basico no fluxo do agente, triagem de maquinas descobertas, TTL de sessao e bloqueio de host ativo sem `RustDesk ID` ja existem; falta aplicar migration/generate/seed no ambiente, validar fluxo fim a fim por perfil, endurecer mensagens de erro e fechar testes operacionais com maquina real.
  - runbook minimo (execucao de ambiente):
    - `npm run db:validate`
    - `npm run db:generate`
    - `npm run db:migrate` (dev)
    - `npm run db:deploy` (producao)
- [ ] `FEAT-002` [Media] Estruturar gestao de ambientes, hosts e agentes para operacao remota. Cadastro inicial de host, descricao persistida, observacoes manuais do host, token de instalacao, heartbeat, metadata basica do agente, configuracoes globais do modulo remoto e regeneracao automatica de `installToken` legado ja existem; falta validar auto-registro/heartbeat em ambiente real, materializar instalacao em campo com ciclo recorrente e decidir se a entidade de agente vira tabela propria quando o recorte OSS-first estabilizar.
- [ ] `FEAT-002A` [Media] Consolidar UX operacional de hosts e agentes. Grid operacional simplificada, cadastro rapido por modal, filtros por status/ambiente/heartbeat, copia rapida de `RustDesk ID`, labels amigaveis, indicadores de prontidao, acesso direto, simplificacao do detalhe do host, edicao de `machineName`, separacao entre observacoes do host vs empresa e matriz compartilhada de status operacional (`ONLINE`, `RECENT`, `OFFLINE`, `MISCONFIGURED`, `SESSION_BUSY`) ja existem; falta fechar consistencia visual entre configuracao, diretorio e detalhe, revisar mobile e reforcar feedback de bloqueios por host.
- [ ] `FEAT-002G` [Media] Refatorar frontend da aba `/portal/configuracoes?tab=remote` para contrato remoto tipado. Status atual: **parcial**. Ja existe parser unico de resposta remota no frontend (`code/message/httpStatus`) aplicado no formulario de configuracoes e nas acoes de credenciais/address-book, incluindo mensagens acionaveis para `401/403/429`. Pendencias para fechar: (1) aplicar o mesmo parser em 100% das actions restantes da aba, (2) validar inputs com a mesma regra do backend para `RustDesk ID` (7-12 digitos), (3) revisar copy final para remover qualquer linguagem residual de onboarding legado.
- [ ] `FEAT-002H` [Media] Refatorar frontend de `/portal/plataforma-remota` para padrao unico de erros e operacao. Status atual: **parcial**. O parser remoto tipado ja foi aplicado em fluxos principais de `directory`, `host-details` e `remote-controls`, com fallback acionavel para `401/403/429`; os estados de `agentToken` e CTA de rebootstrap ja aparecem no diretorio/detalhe. Pendencias para fechar: (1) cobrir 100% das mutacoes (`create/update/delete/link/rotate/revoke/relink/sessions`) com o mesmo parser unificado, (2) completar UX de retry para `429` no fluxo de triagem, (3) concluir sweep de copy OSS-first sem termos residuais antigos.
- [ ] `FEAT-002I` [Baixa] Sweep de copy/UX do modulo remoto para alinhamento de nomenclatura operacional. Criterios de aceite: (1) remover textos residuais de onboarding legado nas telas de configuracao, diretorio e detalhe, (2) padronizar termos oficiais (`discover`, `bootstrap`, `sync`, `ack`, `agentToken`, `host installer required`), (3) revisar toasts/empty states/tooltips para consistencia com a documentacao tecnica.
- [ ] `FEAT-002B` [Media] Fechar fluxo OSS-first de agente. Endpoints de `discover`, `bootstrap`, `sync` e `ack`, rate limit basico por IP, `agentToken` pos-bootstrap e manual tecnico OSS ja existem; o pipeline de release ja foi endurecido com segredo injetado por step, contrato NSIS final (`/DDISCOVERY_TOKEN` + `/DPORTAL_BASE_URL`) e runtime do agente via Registry (`HKLM\Software\Trilink\RemoteAgent`). Falta validar auto-vinculo guiado ao host em campo, validar ciclo recorrente em ambiente real, validar reconfiguracao de maquina ja instalada e decidir a estrategia futura do contrato de descoberta.
- [ ] `FEAT-002C` [Media] Evoluir o agente RustDesk para cenarios `multi-servidores` na mesma maquina. Este recorte esta em fase final: backend ja persiste e sincroniza `sysproUpdates[]` em entidade filha por host, UI ja exibe e permite operacao multiempresa por maquina no diretorio e no detalhe, e os fluxos `discover`/`sync` ja aceitam leitura multi-instalacao. Pendencias para encerramento: abrir lista realmente configuravel de empresas/caminhos Syspro no agente por host, consolidar onboarding do agente para esse modo, validar em campo com discos/bases diferentes e fechar a decisao de produto sobre a entidade filha como objeto operacional completo (alem de leitura de update). **Criterio tecnico obrigatorio**: Smart Scan multi-disco (varredura tatica por pastas `*Syspro*` com profundidade limitada, sem recurse cego na raiz dos discos), com deduplicacao por `empresa+caminho`, envio incremental e snapshot diario completo. A fonte unica deste desenho foi consolidada em `/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss`.
- [ ] `FEAT-002D` [Media] Evoluir o agente remoto para `inventario tecnico` e `telemetria operacional` alem do heartbeat atual. O backend atual ja suporta `serviceStatus` e `sysproUpdates[]`, mas ainda nao persiste nem exibe `IP local`, `modelo do equipamento`, `RAM`, `networkReady`, versao efetiva do binario RustDesk e `status de processo do Syspro` separado da leitura de arquivo. Esse recorte deve abrir contrato de payload para `hardware`, `network`, decidir persistencia em `RemoteDiscoveredHost` e `RemoteHost`, refletir a leitura no detalhe da maquina e definir politica segura para `self-healing` do Syspro como comportamento opcional, nao padrao.
- [ ] `FEAT-002E` [Alta] Concluir endurecimento final do `agentToken` pos-bootstrap e da observabilidade do agente. Status atual: **base tecnica implementada** (token emitido no `bootstrap`, hash persistido, `sync/ack` aceitando apenas `agentToken`, expiracao por politica, sinais `lastHeartbeatSuccessAt/lastHeartbeatErrorAt/lastHeartbeatErrorMessage/lastKnownIp/lastRegisterAt/lastRegisterSource/agentTokenIssuedAt/agentTokenLastUsedAt`, rotacao/revogacao com rebootstrap obrigatorio e exposicao desses sinais no diretorio/detalhe). Pendencias para fechamento: (1) validacao em campo do ciclo completo `bootstrap -> sync -> rotate/revoke -> rebootstrap -> ack` em maquina real, (2) cobertura automatizada minima para lifecycle de token (cenarios valido/invalido/expirado/rotacionado) em `remote-domain` e rotas criticas, (3) varredura final de copy/UX na plataforma remota para remover termos residuais antigos fora do dominio (ex.: `register`/`installer`). Fonte unica: `/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss`.
- [ ] `FEAT-002F` [Alta] Consolidar a API de sincronizacao RustDesk para transformar o portal na fonte de verdade de alias, configuracao e compliance do cliente remoto. A base deste recorte ja foi aberta com os contratos de `bootstrap`, `sync`, `ack` e `address-book`, emissao de `agentToken`, retorno de `hostId`, `alias`, `serverHost`, `apiHost`, `publicKey`, `serverConfig` e `targetVersion`, persistencia dos sinais de compliance do cliente no `remote_host` e fila persistida de acoes do agente para `reapply_alias`, `reapply_config`, `upgrade_client` e `rotate_token_required`, incluindo ciclo de entrega e encerramento por `ack`, telemetria por comando, visao agregada no diretorio por backlog/janela/hotspots, timeline operacional por host/comando (`createdAt`, `deliveredAt`, `executedAt`, `failedAt`, duracao), autenticacao endurecida do `address-book` por credencial persistida com escopo (`GLOBAL`/`COMPANY`), rotacao, revogacao e trilha de uso, formalizacao do papel do `discover` como triagem controlada com regras de transicao explicitas para `bootstrap/sync`, UX operacional de credenciais no portal (filtros, status, expiracao e atores) e endpoint de perfil para cliente customizado (`/api/remote/rustdesk/client-profile`) alinhado ao servidor proprio `acesso.trilinksoftware.com.br`. O que permanece pendente agora e validar em campo o ciclo fim a fim com agente customizado.
- [ ] `FEAT-003` [Media] Abrir fluxo inicial de sessoes remotas com auditoria minima. Start, stop, ticket link, comentario interno no Zammad, regra de deduplicacao inicial, bloqueio de inicio concorrente no mesmo host, `expiresAt` para TTL e recorte tecnico do contrato de auditoria por sessao ja existem; falta validar o fluxo completo em ambiente real, materializar job de expiracao para limpar sessoes vencidas, consolidar a resolucao de host por ticket e persistir a auditoria de solicitacao, inicio, encerramento e origem da sessao.
- [ ] `FEAT-003A` [Media] Integrar `Zammad + RustDesk` com `rustdesk://<id>` no ticket e abertura rapida da sessao pelo atendente. Endpoint de deep-link ja implementado e agora exige sessao `STARTED` valida para o ticket; falta configurar custom link no Zammad e validar com ticket real.
- [ ] `FEAT-003B` [Media] Criar fluxo de webhook e API entre `Zammad` e orquestrador remoto para registrar sessao, ticket vinculado e trilha de auditoria. Webhook ja cria e encerra `RemoteSession`, passa a preencher `expiresAt` e respeita o endurecimento do fluxo; falta validar payload real do Zammad e materializar limpeza automatica de sessoes expiradas. A referencia tecnica vigente esta em `/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/integracao-zammad`.
- [ ] `FEAT-003C` [Media] Refinar UX da operacao de sessoes remotas. Fluxo manual de solicitar, iniciar e encerrar, destaque do ultimo ticket, recomendacao de proximo passo e bloqueios basicos de transicao ja existem; falta aplicar o mesmo padrao visual nas demais telas, validar o fluxo em ambiente real e ampliar o feedback operacional para o atendente.
- [ ] `FEAT-003D` [Media] Evoluir a experiencia de atendimento remoto com feedback operacional completo. A direcao ja esta consolidada para destacar o status do host, recomendar o proximo passo, reforcar bloqueios de transicao, destacar o link do ticket e melhorar o feedback visual para o atendente; falta aplicar esse padrao em todas as telas, validar o comportamento em ambiente real e reforcar a resolucao de host por ticket.
- [ ] `FEAT-004` [Media] Criar shell de credenciais por referencia com controle de acesso e trilha de uso.
- [ ] `FEAT-005` [Media] Implementar painel inicial de backup e restore com jobs catalogados. A direcao ja esta consolidada para agente local com execucao `pull`, agenda por host, `BackupJob`, `BackupArtifact`, envio preferencial para storage proprio e visualizacao rapida no mesmo modulo remoto; faltam modelagem, endpoints, polling do agente e UI operacional.

#### Ideias de implementacao

- Mapa de cobertura remota por UF ou regiao para acompanhar densidade de hosts.
- Timeline de heartbeats por host com historico de latencia, falhas e reinicializacoes.
- Dashboard em tempo real com SSE ou polling curto para status `ACTIVE`, `INACTIVE` e `MAINTENANCE`.
- Historico completo de sessoes inline no detalhe do host, com duracao, operador e ticket vinculado.
- Bulk actions na listagem para trocar empresa, mudar status e distribuir pacotes NSIS em lote.
- Notificacao proativa de host offline quando um host ativo deixar de enviar heartbeat por mais de N minutos.
- Versionamento de agente, mostrando divergencia entre `agentVersion` reportado e versao alvo do modulo.
- Notas ricas por host com markdown, preview e historico de edicao.
- Portal simplificado do cliente para acesso remoto, sem expor configuracoes internas.
- Adicionar fingerprint de hardware no `discover` para evitar colisao por `machineName`.
- Adicionar `statusChangedAt` e `statusChangedByUserId` no host para trilha de alteracoes operacionais.
- Paginar `getRemotePlatformDirectory` com filtros server-side por empresa, status, `agentVersion` e `lastHeartbeatAt`.
- Otimizar `syncRemoteHostSysproUpdates` para batch ou raw SQL quando houver muitas instalacoes.
- Persistir filtros da listagem remota na URL para facilitar compartilhamento e refresh.
- Adicionar badge visual para `serviceStatus` no card do host.
- Desabilitar o botao de conexao RustDesk com explicacao quando o host nao tiver `agentExternalId`.
- Exibir preview e checklist de configuracao (NSIS + Registry) antes da instalacao.
- Permitir filtro de instalacoes Syspro desvinculadas diretamente no detalhe do host.
- `FEAT-RUST-LOCK` [Alta] Bloquear interface de configuracoes para o usuario final em modo kiosk.
- `FEAT-RUST-UAC` [Alta] Garantir auto-elevacao no inicio da sessao.
- `FEAT-RUST-INV` [Media] Integrar coleta de inventario basico de disco, CPU e RAM ao payload do agente.
- `INFRA-RUST-MSI` [Media] Criar pipeline para gerar instalador MSI para deploy em massa.
- `FEAT-RUST-MSG` [Baixa] Customizar mensagens de chat pre-definidas.
- `FEAT-TELEMETRY-AGENT` [Alta] Evoluir agente de telemetria embarcado (sem dependencia de script avulso), com foco em inventario e saude operacional do ERP.
- `INFRA-NSIS-PATCH` [Media] Modificar `patch_client.py` para injetar instrucoes de telemetria no instalador Windows.
- `INFRA-API-HEALTH` [Baixa] Criar endpoint simples no servidor para receber e armazenar payloads de telemetria.
- `FEAT-TELEMETRY-ANALYTICS` [Alta] Configurar dashboard de monitoramento dos dados coletados.
- `FEAT-TELEMETRY-ALERTS` [Media] Configurar alertas para anomalias como aumento de falhas de conexao.
- `FEAT-TELEMETRY-REPORTS` [Baixa] Gerar relatorios periodicos para revisao interna e otimizacao do servico.
- RustDesk customizado: `Service Persistence`, `Hardware Codec Force`, `Custom Icon Set`, `Pre-defined Password`, `Privacy Mode`, `Auto-Cleanup Logs`, `One-Click SOS`, `Headless Terminal Mode`, `Auto-Session Recording`, `White-label Tray Menu`, `Silent Install`, `Clipboard Policy Control`, `Intelligent Relay Selection` e `Custom Canned Chat`.

### Chamados, Zammad e atendimento

#### Backlog de implementacao

- [ ] `FEAT-003E` [Media] Permitir abertura manual de ticket na rota `/portal/chamados`, obrigando o operador a selecionar o email de contato do Zammad vinculado a empresa.
- [ ] `FEAT-007` [Media] Estruturar canal futuro de WhatsApp mediado pelo backend. A direcao arquitetural ja foi definida para `apps/web`, `apps/api`, `apps/whatsapp-bridge`, `Zammad` e `RustDesk`, a base de `CompanyContact` ja entrou no cadastro da empresa, os contratos reforcados de `Conversation`, `ConversationMessage`, `ConversationQueueEvent` e `ConversationAssignment` ja existem no dominio com snapshots, delivery e historico de fila, e a modelagem Prisma base dessas quatro tabelas ja foi aberta; faltam aplicar migration no ambiente, backend dedicado ligado aos casos de uso reais, bridge isolado, filas operacionais reais, trilha auditavel e promocao controlada de contato para `User`. A referencia de trabalho deste recorte esta em `/docs/manuais-tecnicos/backlog-e-pendencias/arquitetura-canal-whatsapp`.
- [ ] `FEAT-007A` [Media] Concluir integracao de WhatsApp via backend dedicado, com filas operacionais reais, trilha auditavel e promocao controlada de contato para `User`. A direcao arquitetural ja esta definida para `apps/web`, `apps/api`, `apps/whatsapp-bridge`, `Zammad` e `RustDesk`; faltam aplicar migration no ambiente, backend dedicado ligado aos casos de uso reais, bridge isolado, filas operacionais reais, trilha auditavel e promocao controlada de contato para `User`.
- [ ] `FEAT-007B` [Media] Evoluir a experiencia de atendimento via WhatsApp com mensagens interativas, templates e feedback operacional completo. A base de modelagem e contratos ja existe; falta fechar backend dedicado, bridge isolado, filas reais, trilha auditavel e promocao controlada de contato para `User`.

#### Ideias de implementacao

- Criacao de chamado inline com `ticketFormSchema` ja existente nos contratos compartilhados.
- Resposta a artigos direto no portal com editor rico, anexo e envio para o Zammad.
- Filtro de chamados por empresa com autocomplete.
- Exportacao CSV de chamados filtrados por status, periodo e fila.
- Agrupamento de status customizavel via settings.
- Badge de SLA diretamente na lista de tickets.
- Marcacao de tickets duplicados com referencia ao ticket original e nota interna no Zammad.
- Assinatura de ticket pelo cliente para receber atualizacoes por email.
- Widget de historico de atendimentos por empresa dentro do cadastro da empresa.
- Cache de camada de dados para reduzir chamadas live ao Zammad em picos.
- Usar `prisma.zammadTicketCache` para calculo de notificacoes e SLA sem varrer tickets em memoria.
- Mover filtros de fechados para server-side no gateway do Zammad.
- Adicionar rate limit nas quick actions de ticket.
- Migrar `getTicketsForCustomerEmailsPaged` para busca POST ou paginacao de query.
- Corrigir pagina da listagem quando filtros mudarem para sempre voltar a pagina 1.
- Separar artigos internos de artigos publicos para cliente no detalhe do chamado.
- Adicionar breadcrumb com filtros preservados no detalhe do ticket.
- Tornar itens do dashboard de chamados clicaveis.
- Criar loading dedicado para a rota de detalhe do chamado.
- Zammad customizado: `Grafana Dashboard`, `AI Ticket Summarizer`, `WhatsApp Multi-Instance`, `Zabbix ou CheckMK Bridge`, `CMDB`, `Time Accounting to ERP`, `Custom CSS Branding`, `Auto-Priority Logic`, `Telegram ou Signal Bot`, `Elasticsearch Tuning`, `SLA Multi-Nivel`, `Knowledge Base Locking`, `PWA Mobile Booster` e `Auto-Closure Survey`.

### App web, tema, API, backend e mobile

#### Backlog de implementacao

- [ ] `FEAT-009` [Media] Criar shell inicial de `apps/api` com endpoints de exemplo e contrato de API compartilhado. O esqueleto do app, configuracao de rota, autenticacao basica por token, logger estruturado e endpoint de exemplo ja existem; faltam definir contrato compartilhado, aplicar a esse contrato em endpoints reais, integrar casos de uso do `apps/web` e validar o comportamento em ambiente real.
- [ ] `FEAT-009A` [Media] Criar shell inicial de backend dedicado para o canal de WhatsApp, com endpoints de exemplo e contrato compartilhado. O esqueleto do app, configuracao de rota, autenticacao basica por token, logger estruturado e endpoint de exemplo ja existem; faltam definir contrato compartilhado, aplicar a esse contrato em endpoints reais, integrar casos de uso do `apps/web` e validar o comportamento em ambiente real.
- [ ] `UI-001` [Media] Reforcar a consistencia visual do app com o tema compartilhado, aplicando-o integralmente e eliminando hardcodes. O tema compartilhado ja existe em `packages/ui`, foi integrado ao `apps/web` e aplicado em diversos componentes e telas, mas ainda existem pontos de inconsistencia visual e hardcodes de cor, tipografia e espacamento que precisam ser eliminados para garantir uma experiencia visual coesa e aderente ao design system.
- [ ] `FEAT-010` [Media] Criar shell inicial de `apps/mobile` com boundaries definidas e integracao inicial com o backend. O esqueleto do app, configuracao de rota, autenticacao basica por token, logger estruturado e tela de exemplo ja existem; faltam definir boundaries de integracao, aplicar esses contratos em telas reais, integrar casos de uso do `apps/web` e validar o comportamento em ambiente real.

#### Ideias de implementacao

- Revisar responsividade e densidade visual das telas operacionais com foco em uso mobile.
- Consolidar contratos compartilhados entre `apps/web`, `apps/api` e `apps/mobile` antes de expandir features novas.

## Proximas sprints por prioridade

### Sprint 1 - Validacao real do remoto

1. `FEAT-001`
   aplicar migrations pendentes, `prisma generate` no ambiente e validar o fluxo completo por perfil
   comandos base:
   - `npm run db:validate`
   - `npm run db:generate`
   - `npm run db:migrate` (dev)
   - `npm run db:deploy` (producao)
2. `FEAT-002B`
   validar `discover`, `bootstrap`, `sync`, `ack`, auto-vinculo por `installToken` e metadata real do agente (foco tecnico em campo)
3. `FEAT-002E`
   validar `agentToken`, expiracao por politica e observabilidade minima do agente em maquina real (foco tecnico em campo)
4. `FEAT-002F`
   validar em campo o ciclo `bootstrap -> sync -> ack` com fila de comandos, compliance e telemetria por host (foco tecnico em campo)
5. `FEAT-003`
   materializar job de expiracao, persistir auditoria por sessao e validar `REQUESTED -> STARTED -> ENDED`
6. `FEAT-003A`
   configurar e validar deep-link real no `Zammad`
7. `FEAT-003B`
   validar payload real do webhook e fechar limpeza automatica de sessoes vencidas

### Sprint 2 - Estabilizacao do agente e UX

1. `FEAT-002G`
   alinhar `/portal/configuracoes?tab=remote` ao contrato tipado (`code/message/httpStatus`) e copy OSS-first
2. `FEAT-002H`
   alinhar `/portal/plataforma-remota` ao contrato tipado, estados de token e UX de rate limit/retry
3. `FEAT-002I`
   limpar copy residual legado e padronizar nomenclatura do modulo remoto
4. `FEAT-002A`
   fechar consistencia visual entre configuracao, diretorio e detalhe e revisar responsividade
5. `FEAT-002F`
   fechar UX operacional das credenciais do `address-book` e consolidar onboarding 100% via bootstrap autenticado
6. `FEAT-002E`
   amadurecer UX operacional/mobile para rotacao, rebootstrap e expiracao de token
7. `FEAT-002B`
   validar reconfiguracao de maquina ja instalada e endurecer segredos do agente (frente de produto/UX)
8. `INFRA-027`
   E2E dos fluxos criticos do portal e docs

Quebra por tela para implementacao:

1. `/portal/configuracoes?tab=remote` (`FEAT-002G`)
2. `/portal/plataforma-remota` (`FEAT-002H`)
3. Copys transversais do modulo remoto (`FEAT-002I`)

Checklist objetivo por componente:

1. `settings-tab.tsx`: tratar `code/message/httpStatus` em salvar configuracoes, com estado de erro por codigo.
2. `settings-tab.tsx`: remover termos legados de script/installer e manter linguagem `bootstrap/sync/ack`.
3. `directory-page.tsx`: mapear erros de `create/update/delete/link/relink` por `code` e exibir feedback acionavel.
4. `host-details-page.tsx`: mapear estados de token (`valid`, `expired`, `rotated`, `revoked`) com CTA de rebootstrap.
5. `remote-controls.tsx`: unificar toasts e mensagens de erro para contrato tipado remoto.
6. `actions`/`hooks` de remoto: normalizar parsing de erro para sempre priorizar `message` e `code` do backend.
7. `copy UX`: remover termos residuais (`register`, `installer legado`, `.ps1` como fluxo principal).
8. `E2E`: cobrir ao menos um fluxo feliz e um fluxo de erro tipado para `configuracoes` e `plataforma-remota`.

### Sprint 3 - Multi-instalacao e observabilidade

1. `FEAT-002C`
   validar multiempresa por maquina com onboarding configuravel do agente e leitura real em campo
2. `FEAT-002D`
   abrir inventario tecnico, rede e telemetria operacional do agente
3. `INFRA-026`
   metricas minimas das rotas e jobs mais sensiveis
4. `FEAT-003C`
   reforcar UX das sessoes com feedback operacional completo

### Sprint 4 - Atendimento e shells dedicados

1. `INFRA-027A`
   ligar `apps/api` aos casos de uso reais
2. `FEAT-007`
   aplicar migration de conversas, abrir queries e actions minimas e preparar backend dedicado
3. `FEAT-009A`
   evoluir backend dedicado do canal WhatsApp
4. `FEAT-004`
   shell de credenciais por referencia
5. `FEAT-005`
   modelagem, endpoints e UI inicial de backup e restore operacional
6. `INFRA-027B`
   materializar runtime real do `apps/mobile`

### Sprint 5 - Docs e experiencia publica

1. `INFRA-028` a `INFRA-034`
   evolucao da experiencia de docs
2. `FEAT-006`
   limpeza final de hardcodes na area publica
3. `UI-001`
   consolidar consistencia visual do app

## Referencias

- arquitetura atual: [`/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/arquitetura-aplicacao-monorepo`](/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/arquitetura-aplicacao-monorepo)
- estrategia remota (fonte unica): [`/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss`](/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss)
- agente remoto OSS: [`/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss`](/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss)
- desenho do `agentToken` e multi-bases remoto: [`/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss`](/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/operacao-agente-remoto-oss)
- integracao `Zammad + RustDesk`: [`/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/integracao-zammad`](/docs/manuais-tecnicos/documentacao-tecnica-arquitetura/integracao-zammad)
- integracao `Zammad + WhatsApp`: [`/docs/manuais-tecnicos/backlog-e-pendencias/arquitetura-canal-whatsapp`](/docs/manuais-tecnicos/backlog-e-pendencias/arquitetura-canal-whatsapp)
















