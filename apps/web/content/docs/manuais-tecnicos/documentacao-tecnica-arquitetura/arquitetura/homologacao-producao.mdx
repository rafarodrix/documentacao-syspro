---
title: DRE
description: Demonstrativo de Resultados do ExercÃ­cio (DRE)
draft: true
lastUpdated: 2026-03-22
owner: Equipe de Documentacao
status: published
tags:
  - manuais-tecnicos
  - documentacao-tecnica-arquitetura
  - homologacao-producao
audience:
  - admin
  - developer
  - suporte
---

import { Card, Cards } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';
import { Accordions, Accordion } from 'fumadocs-ui/components/accordion';
import { Step, Steps } from 'fumadocs-ui/components/steps';

## IntroduÃ§Ã£o

Este manual descreve o procedimento tÃ©cnico para a correÃ§Ã£o de cupons fiscais (NFC-e/SAT) emitidos erroneamente no ambiente de **HomologaÃ§Ã£o** (ambiente de testes, sem validade fiscal) quando deveriam ter sido emitidos no ambiente de **ProduÃ§Ã£o** (ambiente com validade fiscal).

O objetivo deste processo Ã© alterar os registros diretamente no banco de dados para que os documentos fiscais sejam retransmitidos Ã  SEFAZ (Secretaria da Fazenda) no ambiente correto, garantindo a conformidade fiscal da operaÃ§Ã£o.

<Callout type="warning">
**AtenÃ§Ã£o:** Este Ã© um procedimento de nÃ­vel avanÃ§ado que envolve a execuÃ§Ã£o de comandos diretamente no banco de dados. A execuÃ§Ã£o incorreta pode causar inconsistÃªncia de dados e problemas fiscais. Prossiga apenas se vocÃª for um profissional qualificado com acesso e permissÃ£o para alterar o banco de dados.
</Callout>

## Passo a Passo

O processo Ã© dividido em quatro etapas crÃ­ticas que devem ser seguidas na ordem correta.

<Steps>

### 1\. Isolar os Cupons (Status OFF-LINE)

O primeiro passo, realizado na interface do sistema, Ã© alterar o status dos cupons emitidos incorretamente para 'OFF-LINE'. Isso os retira da fila de transmissÃ£o automÃ¡tica e permite a manipulaÃ§Ã£o segura no banco de dados.

1.  Acesse a tela de consulta de lanÃ§amentos ou o PDV (Ponto de Venda).
2.  Localize os cupons que foram emitidos no ambiente de homologaÃ§Ã£o.
3.  Utilize a funÃ§Ã£o do sistema para alterar o status desses cupons para **OFF-LINE**.

### 2\. Alterar o Ambiente na Tabela

Com os cupons em modo 'OFF-LINE', o prÃ³ximo passo Ã© atualizar a tabela `lancamento_xml` para que o sistema entenda que eles pertencem ao ambiente de ProduÃ§Ã£o.

Execute o seguinte comando SQL:

```sql
update lancamento_xml set ambiente = 1
where id_lancamento_xml in (
    select lx.id_lancamento_xml
    from lancamento l
    left join lancamento_xml lx on lx.id_lancamento = l.id_lancamento
    where l.status = 'OFF-LINE'
);
```

**O que este comando faz?**
Ele define o campo `ambiente` como `1` (ProduÃ§Ã£o) para todos os lanÃ§amentos que estÃ£o com o status `OFF-LINE`.

### 3\. Corrigir o Ambiente no ConteÃºdo do XML

Agora, Ã© crucial corrigir a tag `<tpAmb>` dentro do prÃ³prio arquivo XML armazenado no banco de dados. Se este passo for ignorado, o XML serÃ¡ enviado ao ambiente correto, mas seu conteÃºdo ainda indicarÃ¡ ser de homologaÃ§Ã£o, causando rejeiÃ§Ã£o pela SEFAZ.

Execute o seguinte comando SQL:

```sql
UPDATE LANCAMENTO_XML
SET XML = CAST(
    REPLACE(
        CAST(XML AS VARCHAR(32765)),
        '<tpAmb>2</tpAmb>',
        '<tpAmb>1</tpAmb>'
    ) AS BLOB)
WHERE AMBIENTE = 2
AND CSTAT = 100
AND CAST(XML AS VARCHAR(32765)) CONTAINING '<tpAmb>2</tpAmb>'
AND ID IN (
    SELECT FIRST 1000 ID
    FROM LANCAMENTO_XML
    WHERE AMBIENTE = 2 AND CSTAT = 100
);
```

\<Callout type="info"\>
**Processamento em Lotes:** Note que o comando acima estÃ¡ limitado a `FIRST 1000` registros. Caso existam mais de 1000 cupons para corrigir, pode ser necessÃ¡rio executar este script mÃºltiplas vezes atÃ© que todos os registros sejam atualizados.
\</Callout\>

### 4\. ForÃ§ar o Reenvio para a SEFAZ

Por fim, para que o sistema Syspro entenda que precisa reenviar esses cupons que estavam em modo offline, Ã© necessÃ¡rio limpar os registros de data e hora do Ãºltimo envio.

Execute o comando SQL final:

```sql
update empresa_nfe set
    dfe_hr_ultimo_envio_offline = null,
    dfe_dt_ultimo_envio_offline = null;
```

ApÃ³s este comando, o serviÃ§o de transmissÃ£o de documentos fiscais do Syspro irÃ¡ identificar os cupons pendentes e iniciarÃ¡ o processo de envio para o ambiente de ProduÃ§Ã£o.

</Steps>

## Perguntas Frequentes

<Accordions>
<Accordion title="Posso executar este procedimento com o sistema em uso?">
NÃ£o Ã© recomendado. O ideal Ã© realizar o procedimento em um horÃ¡rio de baixa movimentaÃ§Ã£o ou com os terminais de venda (PDVs) pausados para evitar conflitos de sincronizaÃ§Ã£o e novos registros sendo gravados enquanto a manutenÃ§Ã£o Ã© realizada.
</Accordion>
<Accordion title="O que acontece se eu pular o passo de alterar o conteÃºdo do XML (Passo 3)?">
Se o passo 3 for pulado, o sistema enviarÃ¡ o cupom para o webservice de ProduÃ§Ã£o da SEFAZ, mas o conteÃºdo do XML ainda terÃ¡ a tag `&lt;tpAmb&gt;2&lt;/tpAmb&gt;` (HomologaÃ§Ã£o). A SEFAZ irÃ¡ validar o conteÃºdo, identificar a inconsistÃªncia de ambiente e **rejeitarÃ¡** o documento fiscal.
</Accordion>
<Accordion title="Como posso verificar se o processo funcionou corretamente?">
ApÃ³s forÃ§ar o reenvio (Passo 4), acompanhe o status dos cupons na tela de gerenciamento de documentos fiscais do Syspro. Eles devem sair do status 'OFF-LINE' e, apÃ³s a comunicaÃ§Ã£o com a SEFAZ, obter o status 'AUTORIZADO'. VocÃª tambÃ©m pode consultar o XML de um dos cupons corrigidos para confirmar que a tag `&lt;tpAmb&gt;` estÃ¡ com o valor `1`.
</Accordion>
<Accordion title="Este procedimento corrige a numeraÃ§Ã£o do cupom que foi usada em homologaÃ§Ã£o?">
NÃ£o. O procedimento apenas corrige o ambiente de emissÃ£o para que o documento seja fiscalmente vÃ¡lido. A numeraÃ§Ã£o do cupom fiscal (NFC-e) que foi utilizada (ou "queimada") no ambiente de homologaÃ§Ã£o serÃ¡ perdida na sequÃªncia de produÃ§Ã£o, resultando em um pulo na numeraÃ§Ã£o. Isso Ã© esperado e deve ser justificado em caso de fiscalizaÃ§Ã£o.
</Accordion>
</Accordions>



