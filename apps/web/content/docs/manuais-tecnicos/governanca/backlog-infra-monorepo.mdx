---
title: Backlog de Infraestrutura e Estabilizacao
description: Backlog priorizado com os itens pendentes para estabilizacao do app, consolidacao arquitetural e evolucao das novas funcionalidades.
lastUpdated: 2026-03-23
owner: Engenharia de Plataforma
status: published
---

## Objetivo

Concentrar apenas o backlog ainda pendente, separando:

- estabilizacao do app atual
- infraestrutura e qualidade
- novas funcionalidades em construcao

## Contexto atual

Ja concluido:

- Fase A do modular monolith disciplinado
- EPIC B1 de workspace e shell
- `INFRA-013` com o primeiro recorte de `packages/shared`
- `INFRA-014` com o primeiro recorte de `packages/ui`
- `INFRA-015` com o primeiro recorte de `packages/contracts`
- `INFRA-016` com o primeiro recorte de `packages/core`
- `INFRA-017` com `packages/database`
- `INFRA-018` com a estrategia de repositories/adapters para persistencia
- `INFRA-019` com o shell modular de `packages/api`
- `INFRA-020` com o padrao oficial de data fetching e invalidacao
- `INFRA-021` com o shell estrutural de `apps/mobile`
- `INFRA-022` com o shell de `apps/api` e transporte HTTP minimo
- drenagem de `apps/web/src/core`
- drenagem de `apps/web/src/hooks`

Estado atual do codigo:

- boundaries por feature estao coerentes
- `src/core` e `src/hooks` ja nao fazem parte da estrutura ativa
- `src/lib`, `src/providers` e `src/types` seguem validos como camadas transversais
- a feature `remote` ja saiu do shell puro e entrou em fase operacional inicial
- a experiencia da feature `remote` ja foi separada em:
  - `Configuracoes > Acesso Remoto`
  - `/app/plataforma-remota`
- a UX operacional inicial da feature `remote` ja contempla:
  - grid simplificada por empresa, `RustDesk ID` e descricao da maquina
  - cadastro rapido recolhivel de maquina
  - copia rapida de `RustDesk ID` e do comando PowerShell
  - download de script `.ps1` por host com nome amigavel
  - geracao automatica de `installToken` para host legado no download do instalador
  - detalhe do host com prontidao operacional e separacao entre observacoes do host vs empresa

## Diretriz de execucao

Ordem recomendada:

1. estabilizar o app web atual
2. reforcar tipagem, tema e consistencia visual
3. consolidar CI, observabilidade e contratos
4. fechar o MVP remoto fim a fim
5. conectar shells de multi-app a casos de uso reais

## Estabilizacao do app atual

- [x] `INFRA-022A` [Alta] Remover hardcodes de tema/cor na area publica e consolidar uso de tokens (`bg-background`, `text-foreground`, `border-border`). Superficies, contraste base e fundos da home foram alinhados aos tokens do tema; acentos visuais de marca permanecem intencionais.
- [x] `INFRA-022B` [Alta] Limpar mojibake residual em `auth`, `site`, `documentos` e componentes adjacentes.
- [ ] `INFRA-022C` [Alta] Endurecer contratos entre `actions`, `hooks`, `forms` e componentes, removendo `any`, casts frouxos e respostas inconsistentes. Recorte inicial em `auth`, `documentos` e `user-access` ja aplicado com contratos discriminados de retorno, tipagem mais estrita de tabelas/forms e remocao de `as any` em hooks e actions; falta expandir o mesmo endurecimento para `company`, `tickets` e ferramentas legadas.
- [ ] `INFRA-022D` [Media] Revisar `src/lib` e mover helpers de dominio que ainda fazem mais sentido dentro de `features/*`.
- [ ] `INFRA-022E` [Media] Normalizar tipagens e formatos compartilhados de UI para reduzir regressao em deploy por incompatibilidade de retorno.
- [x] `INFRA-022F` [Alta] Aplicar `React cache()` em `getProtectedSession()` para evitar consultas repetidas de sessao/usuario/membership no mesmo render tree SSR.
- [x] `INFRA-022G` [Alta] Extrair queries do dashboard em `apps/web/src/app/(platform)/app/page.tsx` para `features/dashboard/application/queries.ts`, removendo acesso direto a Prisma da camada de UI.
- [x] `INFRA-022H` [Media] Aplicar `unstable_cache` em queries frequentes de contagem, settings e status operacionais com invalidacao por tags.
- [x] `INFRA-022I` [Alta] Tornar o fallback do Zammad transparente usando `ZammadTicketCache`, reduzindo SPOF e evitando alerta ruidoso quando o cache recente estiver valido.

## Qualidade, CI e observabilidade

- [ ] `INFRA-023` [Alta] Pipeline CI (lint, typecheck, test, build).
- [ ] `INFRA-024` [Alta] Verificacao de contrato/schema em PR.
- [ ] `INFRA-025` [Alta] Logs estruturados e correlation id.
- [ ] `INFRA-026` [Alta] Metricas minimas por rota critica.
- [ ] `INFRA-027` [Alta] Testes E2E para fluxos criticos do app e docs.

## Integracao progressiva de shells

- [ ] `INFRA-027A` [Media] Ligar `apps/api` aos casos de uso reais do `packages/api`.
- [ ] `INFRA-027B` [Media] Materializar runtime real de `apps/mobile` com boundaries ja definidos.
- [ ] `INFRA-027C` [Media] Revisar o que ainda deve sair de `apps/web/src/lib` para consumo compartilhado via packages.

## Experiencia em docs

- [ ] `INFRA-028` [Media] Historico de buscas no modal.
- [ ] `INFRA-029` [Media] Filtro de busca por categoria/tag.
- [ ] `INFRA-030` [Media] `sitemap.xml` automatico para docs.
- [ ] `INFRA-031` [Media] Analytics de paginas e buscas sem resultado.
- [ ] `INFRA-032` [Media] Copy button com feedback animado.
- [ ] `INFRA-033` [Media] Feedback "Esse conteudo ajudou?" com persistencia.
- [ ] `INFRA-034` [Media] Acessibilidade WCAG AA na rota `/docs`.

## Novas funcionalidades

- [ ] `FEAT-001` [Alta] Materializar MVP de plataforma remota com entidades-base, persistencia e rotas iniciais. Persistencia, tenant enforcement, rotas de `hosts` e `sessions`, separacao entre configuracao e operacao, detalhe do host, CRUD basico de host com `RustDesk ID`, busca operacional do modulo, rate limit basico no fluxo do agente, TTL de sessao e bloqueio de host ativo sem `RustDesk ID` ja existem; falta aplicar migration/generate/seed no ambiente, validar fluxo fim a fim por perfil e endurecer regras operacionais e mensagens de erro.
- [x] `FEAT-001A` [Alta] Definir matriz de acesso da plataforma remota: `ADMIN`, `SUPORTE` e `DEVELOPER` com visao global; `CLIENTE_ADMIN` apenas com visao da propria empresa.
- [ ] `FEAT-002` [Media] Estruturar gestao de ambientes, hosts e agentes para operacao remota. Cadastro inicial de host, descricao persistida, observacoes manuais do host, token de instalacao, heartbeat, metadata basica do agente, download do instalador por host e regeneracao automatica de `installToken` legado ja existem; falta validar auto-registro/heartbeat em ambiente real, materializar fluxo de instalacao do agente em campo, ampliar filtros operacionais e separar entidade de agente quando o recorte OSS-first estabilizar.
- [ ] `FEAT-002A` [Media] Consolidar UX operacional de hosts e agentes. Grid operacional simplificada, cadastro rapido recolhivel de maquina, filtros por status/ambiente/heartbeat, copia rapida de `RustDesk ID`, copia de comando PowerShell, download do script por host, labels amigaveis, indicadores de prontidao, acesso direto, modal curto de conexao e separacao entre observacoes do host vs empresa ja existem; falta unificar o mesmo padrao visual entre configuracao, diretorio e detalhe, reforcar feedback de bloqueios por host e reduzir atrito no fluxo diario do suporte.
- [ ] `FEAT-002B` [Media] Fechar fluxo OSS-first de agente. Endpoints de `register` e `heartbeat`, rate limit basico por IP, script PowerShell base do agente, download de `.ps1` por host e manual tecnico OSS ja existem; falta materializar auto-vinculo guiado ao host, instalar heartbeat periodico em ambiente real, validar atualizacao automatica de `RustDesk ID`/metadata e endurecer o segredo de `installToken` antes de abrir maior escala.
- [ ] `FEAT-003` [Media] Abrir fluxo inicial de sessoes remotas com auditoria minima. Start/stop, ticket link, comentario interno no Zammad, regra de deduplicacao inicial, bloqueio de inicio concorrente no mesmo host e `expiresAt` para TTL ja existem; falta validar o fluxo completo em ambiente real, materializar job de expiracao, fechar resolucao de host por ticket e ampliar auditoria por sessao.
- [ ] `FEAT-003C` [Media] Refinar UX da operacao de sessoes remotas. Fluxo manual de solicitar/iniciar/encerrar, destaque do ultimo ticket, recomendacao de proximo passo e bloqueios basicos de transicao ja existem; falta aplicar o mesmo padrao visual nas demais telas, validar o fluxo em ambiente real e ampliar o feedback operacional para o atendente.
- [ ] `FEAT-003A` [Media] Integrar `Zammad + RustDesk` com `rustdesk://<id>` no ticket e abertura rapida da sessao pelo atendente. Endpoint de deep-link ja implementado e agora exige sessao `STARTED` valida para o ticket; falta configurar custom link no Zammad e validar com ticket real.
- [ ] `FEAT-003B` [Media] Criar fluxo de webhook/API entre `Zammad` e orquestrador remoto para registrar sessao, ticket vinculado e trilha de auditoria. Webhook ja cria/encerra `RemoteSession`, passa a preencher `expiresAt` e respeita o endurecimento do fluxo; falta validar payload real do Zammad e materializar limpeza automatica de sessoes expiradas.
- [ ] `FEAT-004` [Media] Criar shell de credenciais por referencia com controle de acesso e trilha de uso.
- [ ] `FEAT-005` [Media] Implementar painel inicial de backup e restore com jobs catalogados.
- [ ] `FEAT-006` [Baixa] Evoluir area publica para aderir integralmente aos tokens de tema e remover hardcodes restantes.

## Roadmap recomendado

### Sprint 4 - Estabilizacao e confiabilidade

- `INFRA-022A` a `INFRA-022E` estabilizacao do app atual
- `INFRA-023` a `INFRA-027` qualidade e observabilidade
- `INFRA-027A` a `INFRA-027C` integracao progressiva de shells
- `INFRA-028` a `INFRA-034` evolucao de docs

### Sprint 5 - Plataforma remota

- `FEAT-001` consolidacao do MVP remoto
- `FEAT-001A` tenant scope global vs empresa concluido
- `FEAT-002` enriquecimento de gestao de hosts e ambientes
- `FEAT-002A` consolidacao da UX operacional de hosts/agentes
- `FEAT-002B` instalacao e auto-registro OSS-first do agente
- `FEAT-003` consolidacao de sessoes remotas com auditoria
- `FEAT-003C` refinamento da UX operacional de sessoes
- `FEAT-003A` link rapido `Zammad -> RustDesk`
- `FEAT-003B` webhook `Zammad -> remote`
- `FEAT-004` credenciais por referencia
- `FEAT-005` backup e restore operacional

## Proximo passo recomendado

1. executar `INFRA-022A` e `INFRA-022B`
2. depois fechar `INFRA-022C`
3. em seguida validar `FEAT-001` fim a fim com migration, generate e seed

## Proximos passos de produto

1. aplicar a migration mais recente, rodar `prisma generate` e validar o schema remoto no ambiente, incluindo `description`, `notes`, `installToken`, `expiresAt` e metadata do agente
2. validar fim a fim o CRUD de hosts no ambiente real, incluindo cadastro rapido recolhivel, download do instalador, copia de comando PowerShell, edicao, exclusao, `RustDesk ID` e observacoes do host
3. validar `FEAT-002B` em ambiente real com o script OSS-first do agente, auto-vinculo por `installToken`, atualizacao de `RustDesk ID`, heartbeat periodico e resposta de rate limit
4. consolidar o mesmo padrao visual de `FEAT-002A` entre configuracao, diretorio e detalhe, reforcando feedback de bloqueios por host e prontidao operacional
5. validar o fluxo completo de sessao remota com `REQUESTED -> STARTED -> ENDED`, incluindo perfis, bloqueios, ultimo ticket associado, recomendacao operacional, `expiresAt`, deep-link condicionado a sessao ativa e comentario interno no `Zammad`, depois materializar job de expiracao e abrir `AuditLog`, `FEAT-004` e `FEAT-005`

## Proximos passos imediatos do remoto

1. aplicar a migration de `expiresAt` no ambiente
2. validar `429` em `register/heartbeat`
3. validar `REQUESTED -> STARTED -> ENDED` com TTL
4. criar o job de expiracao automatica das sessoes
5. endurecer `installToken` como segredo real, sem depender de token cru na UI/script

## Prioridade sugerida de execucao

1. `FEAT-002B`: rate limit consolidado e endurecimento de `installToken`
2. `INFRA-022F`: `React cache()` em `getProtectedSession()`
3. `INFRA-022G`: extracao das queries do dashboard para `features`
4. `FEAT-003`: TTL e expiracao automatica das sessoes remotas
5. `INFRA-022H`: `unstable_cache` para contagens, settings e status raramente mutaveis
6. `FEAT-001`: validacao operacional de `agentExternalId` em hosts ativos e demais regras de confiabilidade



