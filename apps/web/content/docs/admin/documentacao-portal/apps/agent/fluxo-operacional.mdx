---
title: Agent - Fluxo Operacional
description: Ciclo de vida do agente reorientado para device identity, installation identity e capabilities gerenciadas pelo core.
lastUpdated: 2026-07-17
owner: Engenharia de Plataforma
status: published
tags:
  - agent
  - go
  - heartbeat
---

# Agent - Fluxo Operacional

> Como o agente resolve identidade, registra a instalação local e reconcilia capabilities no portal. Atualizado em: 2026-07-17

---

## Ciclo de vida completo

```text
Instalacao
  -> Device identity
  -> Agent installation identity
  -> Register / heartbeat base
  -> Discovery da capability remota
  -> Vinculo no portal
  -> Bootstrap autenticado da capability remota
  -> Sync continuo
  -> ACK de comandos
```

## Regra central do novo escopo

O agente não usa mais a identidade remota como eixo principal do sistema.

- `device_id` representa a máquina física
- `agent_instance_id` representa uma instalação local específica do agent
- `credential_id` representa a credencial ativa dessa instalação
- RustDesk passa a ser uma capability gerenciada pelo core, não a identidade do agente

## Estado atual do corte

Este corte já está ativo no código:

1. o runtime local persiste `identity.json`
2. o runtime local persiste `installation.json`
3. `register` e `heartbeat` já publicam `deviceId`, `agentInstanceId` e `credentialId`
4. o backend já grava esses campos em `Device` e `AgentInstallation`
5. o vínculo operacional remoto agora é materializado em `AgentCapability`

O fluxo base do agent passa a usar apenas `Device`, `AgentInstallation` e `AgentCapability`.

---

## Fase 1 - Device identity e installation identity

O bootstrap local do `agent-service` agora cria duas camadas de identidade:

```text
identity.json
  -> device_id
  -> hostname
  -> os
  -> identity_source

installation.json
  -> agent_instance_id
  -> credential_id
  -> installed_at
```

`device_id` continua estável para a máquina. Já `agent_instance_id` muda quando uma instalação nova for criada a partir de um runtime limpo.

---

## Fase 2 - Registro e heartbeat base da instalação

O heartbeat base representa a presença da instalação, não o estado do RustDesk.

### Payload atual de register

```json
{
  "deviceId": "WIN-ABC123",
  "agentInstanceId": "install_abc123",
  "credentialId": "cred_abc123",
  "hostname": "PC-CLIENTE-01",
  "os": "windows",
  "identitySource": "machine-guid",
  "agentVersion": "go-agent-v1"
}
```

### Payload atual de heartbeat

```json
{
  "deviceId": "WIN-ABC123",
  "agentInstanceId": "install_abc123",
  "credentialId": "cred_abc123",
  "agentVersion": "go-agent-v1",
  "at": "2026-07-17T12:00:00.000Z"
}
```

### Efeito persistido hoje

- Atualiza a presença da instalação em `AgentInstallation`
- Mantém `lastHeartbeatAt` do cadastro base
- Não publica snapshots operacionais do host remoto
- Não substitui `sync` da capability remota

---

## Fase 3 - Discovery da capability remota

O discovery continua existindo, mas agora deve ser lido como parte da capability remota, não da identidade da instalação.

### Payload enviado

```json
{
  "schemaVersion": "discover.payload.v1",
  "discoveryToken": "disc_abc123",
  "rustdeskId": "123456789",
  "machineName": "PC-CLIENTE-01",
  "agentVersion": "go-agent-v1"
}
```

### Respostas operacionais

| `bootstrapFlow` | Significado | Proximo passo |
| --- | --- | --- |
| `pending_link` | Maquina descoberta, mas ainda sem host vinculado | Aguardar vinculo |
| `linked_host_detected` | O `RemoteDiscoveredHost` ja esta vinculado no portal e segue aguardando ou reaproveitando bootstrap | Aguardar `installToken` |
| `host_bootstrap_required` | O `RemoteDiscoveredHost` ja vinculado requer bootstrap autenticado | Bootstrap |
| `token_invalid` | O `RemoteDiscoveredHost` ja vinculado tem credencial remota invalida ou rotacionada | Bootstrap |

Se o host tiver sido removido do portal, o discovery deixa de rematerializar automaticamente a capability remota. A reinstalação ou a reautorização passa a recriar a instalação e, depois, reexecutar o fluxo remoto.

O backend também não deve usar `rustdeskId` como identidade principal do agente. Ele vale só como identificador externo da capability remota.

O vinculo manual agora pode ser concluido por dois caminhos do portal:

- `Infraestrutura > Hosts`, pelo detalhe do host
- `Infraestrutura > Agentes`, pelo detalhe do dispositivo

Nos dois casos o backend continua sendo a fonte única do vínculo operacional enquanto o split completo de entidades não chega ao banco.

---

## Fase 4 - Bootstrap da capability remota

O `installToken` e o `agentToken` passam a ser credenciais da capability remota.

Se o `discover` nao devolver `installToken`, o agente espera o proximo
ciclo e nao tenta reaproveitar token local legado.

O backend agora so aceita `bootstrap` quando esse `installToken` estiver
amparado por um `discover` recente da mesma maquina vinculada. Token de
instalacao sem triagem recente, ou vindo de identidade divergente, e rejeitado.

Cada `discover` que exigir bootstrap emite um novo `installToken`, e o token
anterior deixa de valer. Depois do bootstrap bem-sucedido, esse `installToken`
tambem e invalidado no portal.

### Payload enviado

```json
{
  "installToken": "rhost_...",
  "rustdeskId": "123456789",
  "machineName": "PC-CLIENTE-01",
  "agentVersion": "go-agent-v1",
  "currentAlias": "PC-CLIENTE-01"
}
```

### Resposta esperada

```json
{
  "agentToken": "agt_...",
  "agentTokenExpiresAt": "2027-05-05T00:00:00Z",
  "serverHost": "relay.trilink.com.br",
  "apiHost": "api.trilink.com.br",
  "alias": "PC-CLIENTE-01"
}
```

O `agentToken` continua protegido localmente com DPAPI, mas não representa a instalação inteira do agent. Ele representa só a comunicação remota desse host/capability com o portal.

O estado remoto persistido pelo agent tambem protege com DPAPI as senhas
locais do RustDesk usadas pelo runtime (`default_password` e
`runtime_password`), reduzindo exposicao de credenciais no disco.

O runtime agora valida explicitamente os contratos remotos devolvidos pelo
portal (`discover.v2`, `rustdesk.bootstrap.v1` e `rustdesk.sync.v1`). Resposta
incompleta, versao inesperada ou `installToken` fora de fluxo esperado passam
a falhar fechado e entram no estado estruturado de erro local.

---

## Fase 5 - Reconcile e sync das capabilities

O reconcile executa em ciclos e roda os modulos em ordem:

1. `device`
2. `remote`
3. `tunnel`
4. `backup`
5. `support`

### Device module

Coleta snapshots como:

- `agentMetrics`
- `diskSnapshot`
- `sysproProcesses`
- `sysproVersions`
- `windowsUpdateStatus`
- `hardwareIdentity`

### Remote module

Enquanto a capability remota ainda mora em `internal/modules/remote`, ela envia `POST /api/remote/rustdesk/sync` com:

- `agentToken`
- `rustdeskId`
- `agentMetrics`
- snapshots de disco, processos e versoes
- `sysproUpdates`

### Efeito persistido no portal

- Atualiza `RemoteHost.lastHeartbeatAt`
- Atualiza `RemoteHost.lastHeartbeatSuccessAt`
- Atualiza `RemoteHost.lastAgentMetrics`
- Atualiza snapshots operacionais do host
- Reentrega comandos pendentes e registra comandos entregues

No runtime local, os eventos criticos de operacao agora entram em uma
`telemetry_outbox.json` persistida. O agent deixa de depender apenas de buffer
em memoria para manter a trilha de `discover`, `bootstrap`, `sync`,
`heartbeat` e `reconcile`.

---

## Fase 6 - ACK de comandos

Depois de executar comandos devolvidos pelo `sync`, o agente envia:

```json
{
  "schemaVersion": "ack.payload.v1",
  "agentToken": "agt_...",
  "commandId": "cmd_001",
  "status": "ACKNOWLEDGED",
  "reasonCode": "COMMAND_PROCESSED",
  "message": "Configuracao reaplicada com sucesso"
}
```

Se o ACK falhar, o agent grava a confirmacao em `pending_ack_queue.json` com
token protegido, backoff persistido, proxima tentativa e ultimo erro
estruturado. O replay deixa de depender apenas do processo atual.

---

## Por que um host pode aparecer sem metricas

1. A instalação existe, mas a capability remota nunca completou bootstrap e, portanto, não possui `agentToken`.
2. O `sync` nao esta executando com sucesso.
3. O portal ainda nao devolveu `installToken` depois do vinculo.
4. O host esta offline ha muito tempo e nunca chegou a publicar snapshots validos.

---

## Leitura operacional no portal

| Sinal | Origem real |
| --- | --- |
| Ultimo contato da instalação | `AgentInstallation.lastHeartbeatAt` |
| Ultimo contato da maquina remota | `RemoteHost.lastHeartbeatAt` |
| CPU e RAM do host remoto | `RemoteHost.lastAgentMetrics` |
| Status de convergencia RustDesk | resposta e compliance do `sync` |

---

## Comandos remotos suportados

| Tipo | Efeito |
| --- | --- |
| `REAPPLY_ALIAS` | Reaplica alias do RustDesk |
| `REAPPLY_CONFIG` | Reaplica configuracao esperada do RustDesk |
| `UPGRADE_CLIENT` | Baixa e instala nova versao do RustDesk |
| `ROTATE_TOKEN_REQUIRED` | Solicita novo bootstrap/token no proximo ciclo |
