---
title: Banco de Dados Schema Overview
description: PostgreSQL via Prisma ORM agrupamento por domi­nio de todos os modelos, enums principais e convenÃ§Ãµes do schema do Trilink Portal.
lastUpdated: 2026-05-18
owner: Engenharia de Plataforma
status: published
tags:
  - banco-dados
  - prisma
  - postgresql
  - schema
---

> PostgreSQL via Prisma ORM. Schema em `packages/database/prisma/schema.prisma`.
> Atualizado em: 2026-05-05

---

## Agrupamento por domÃ­nio

### Empresas e Contratos

```
Company
 ├── CompanyContactCompanyLink[] (vÃ­nculos de contatos com ordem/prioridade)
 ├── Membership[]           (usuÃ¡rios vinculados)
 ├── UserContactLink[]      (espelho do contato principal do usuÃ¡rio por empresa)
 ├── Contract[]             (contratos de serviÃ§o)
 ├── RemoteHost[]           (hosts remotos)
 ├── Conversation[]         (conversas/atendimentos)
 ├── CrmLead[]              (leads CRM)
  ├── AgentInstallation[]    (instalacoes ativas do agent)
  ├── AgentCapability[]      (capabilities gerenciadas pelo agent)
```

**Company â€” campos principais:**
- `cnpj`, `razaoSocial`, `nomeFantasia`, `inscricaoEstadual`
- `status`: ACTIVE | INACTIVE | SUSPENDED | PENDING_DOCS
- `segment`: tipo de negocio (enum)
- `regimeTributario`: Simples Nacional | Lucro Presumido | Lucro Real | MEI
- `parentCompanyId`: referência à empresa matriz (hierarquia)
- `installationDirectory`: caminho do Syspro Server
- `remoteConnections`: conexões remotas normalizadas em JSON

---

### Usuários e Acesso

```
User
  ├── Membership[]           (vÃ­nculos com empresas)
  ├── CompanyContact?        (contato associado)
  ├── UserContactLink[]      (contato efetivo por empresa)

Membership
  ├── User
  ├── Company
  ├── role: Role             (ADMIN|SUPORTE|CLIENTE_ADMIN|CLIENTE_USER|DEVELOPER)

CompanyContact
  ├── CompanyContactCompanyLink[] (M:N com empresas)
  ├── User[]                      (usuários ligados ao contato)
  ├── UserContactLink[]           (espelho por empresa)

AccessProfile
  ├── AccessProfilePermission[]
  ├── UserAccessProfile[]

Permission
 â””â”€â”€ AccessProfilePermission[]
```

**Fluxo real de escopo entre mÃ³dulos:**
1. `User.contactId` aponta para um `CompanyContact`
2. `CompanyContact.companyLinks` define as empresas vÃ¡lidas para esse contato
3. `UserContactAccessService` replica isso em `Membership` e `UserContactLink`
4. `AuthorizationService` resolve permissÃµes globais e por empresa a partir desses vÃ­nculos

---

### Acesso Remoto

```
RemoteHost
  ├── Company
  ├── RemoteAgentCommand[]   (fila de comandos)
  ├── RemoteSession[]        (histÃ³rico de sessÃµes)
  ├── RemoteHostSysproUpdate[] (versÃµes detectadas)

RemoteDiscoveredHost        (hosts ainda nÃ£o vinculados)
  ├── Company?

RemoteSession
  ├── RemoteHost
  ├── User                   (tÃ©cnico)
  ├── Ticket?                (ticket vinculado, opcional)

RemoteAddressBookCredential
  ├── User                   (criador)
  ├── Company?               (escopo COMPANY, opcional)
```

**RemoteHost â€” campos principais:**
- `rustdeskId`: ID no RustDesk (sem espaÃ§os, Ãºnico)
- `hostname`: nome Windows da mÃ¡quina
- `agentToken`: token do agente (persistido com hash)
- `agentTokenExpiresAt`: expiraÃ§Ã£o do token
- `lastHeartbeatAt`: Ãºltimo contato do agente
- `status`: ACTIVE | INACTIVE | MAINTENANCE

**RemoteAgentCommand â€” campos principais:**
- `type`: REAPPLY_ALIAS | REAPPLY_CONFIG | UPGRADE_CLIENT | ROTATE_TOKEN_REQUIRED
- `status`: PENDING | DELIVERED | ACKNOWLEDGED | CANCELLED | FAILED
- `result`: resultado apÃ³s ACK do agente

---

### Mensageria, Atendimento e Tickets

> **ConvenÃ§Ã£o importante:** O modelo `Conversation` Ã© usado tanto para mensageria (WhatsApp, Email) quanto para tickets internos. A distinÃ§Ã£o Ã© feita pelo campo `channel`:
> - `channel: PORTAL` â†’ **ticket interno** (chamado tÃ©cnico)
> - `channel: WHATSAPP | EMAIL | PHONE` â†’ conversa de atendimento/mensageria

```
Conversation
 â”œâ”€â”€ Company
 â”œâ”€â”€ CompanyContact               (cliente que abriu o chamado)
 â”œâ”€â”€ ConversationMessage[]        (mensagens e respostas)
 â”œâ”€â”€ ConversationAssignment[]     (atribuiÃ§Ãµes de responsÃ¡vel)
 â””â”€â”€ ChatwootCsatRating?          (pesquisa de satisfaÃ§Ã£o pÃ³s-fechamento)

ConversationMessage
 â”œâ”€â”€ Conversation
 â”œâ”€â”€ type: TEXT | IMAGE | DOCUMENT | AUDIO | VIDEO
 â””â”€â”€ direction: INBOUND | OUTBOUND

TicketCategory                   (categorias configurÃ¡veis, independente)

ChatwootCsatRating
 â””â”€â”€ Conversation
```

**Conversation â€” campos principais:**

| Campo | Tipo | DescriÃ§Ã£o |
|-------|------|-----------|
| `channel` | enum | `WHATSAPP \| EMAIL \| PORTAL \| PHONE` |
| `status` | enum | Estado atual no workflow (ver abaixo) |
| `priority` | string | `LOW \| NORMAL \| HIGH \| CRITICAL` |
| `team` | string | Equipe responsÃ¡vel (`SUPORTE \| DESENVOLVIMENTO`) |
| `assignedUserId` | string? | UsuÃ¡rio responsÃ¡vel pelo ticket |
| `externalId` | string? | ID no Chatwoot (sincronizaÃ§Ã£o bidirecional) |
| `slaResponseDueAt` | DateTime? | Prazo de primeira resposta |
| `slaResolutionDueAt` | DateTime? | Prazo de resoluÃ§Ã£o |
| `slaResponseHitAt` | DateTime? | Quando a primeira resposta ocorreu |
| `slaResolutionHitAt` | DateTime? | Quando o ticket foi resolvido |
| `metadata` | JSON | Dados extras: `source`, `chatwootConversationId`, etc. |
| `resolvedAt` | DateTime? | Timestamp de resoluÃ§Ã£o |

**ConversationStatus â€” workflow de tickets:**

```
NEW â†’ UNASSIGNED â†’ TRIAGE â†’ IN_PROGRESS â†’ TESTING â†’ RESOLVED â†’ ARCHIVED
                                â”‚                        â”‚
                     WAITING_CUSTOMER          WAITING_INTERNAL
```

---

### CRM

```
CrmLead
 â”œâ”€â”€ Company                (empresa do lead)
 â”œâ”€â”€ CrmActivity[]          (atividades relacionadas)
 â””â”€â”€ CrmTask[]              (tarefas)

CrmActivity
 â””â”€â”€ CrmLead

CrmTask
 â””â”€â”€ CrmLead
```

**CrmLead â€” stage pipeline:**
```
LEAD â†’ MQL â†’ SQL â†’ PROPOSAL â†’ WON
                            â””â†’ LOST
```

---

### Fiscal e TributaÃ§Ã£o

```
TaxNcm                      (tabela NCM completa)
TaxCST                      (CÃ³digo de SituaÃ§Ã£o TributÃ¡ria)
TaxClassification           (classificaÃ§Ãµes fiscais)
SefazStatus                 (status dos webservices por UF)
TaxSyncJob                  (jobs de sincronizaÃ§Ã£o)
```

---

### ConfiguraÃ§Ã£o

```
DocumentoConfig             (tipos de documentos por empresa)
SystemSetting               (configuraÃ§Ãµes key-value globais)
```

---

## Enums de referÃªncia rÃ¡pida

```prisma
enum Role               { ADMIN SUPORTE CLIENTE_ADMIN CLIENTE_USER DEVELOPER }
enum CompanyStatus      { ACTIVE INACTIVE SUSPENDED PENDING_DOCS }
enum RemoteHostStatus   { ACTIVE INACTIVE MAINTENANCE }
enum RemoteSessionStatus { REQUESTED STARTED ENDED FAILED CANCELLED }
enum ConversationChannel { WHATSAPP EMAIL PORTAL PHONE }
enum ConversationStatus  { NEW UNASSIGNED TRIAGE IN_PROGRESS WAITING_CUSTOMER WAITING_INTERNAL TESTING RESOLVED ARCHIVED }
enum CrmLeadStage       { LEAD MQL SQL PROPOSAL NEGOTIATION WON LOST }
enum CrmActivityType    { NOTE CALL MEETING EMAIL WHATSAPP SYSTEM_EVENT }
enum SefazServiceType   { NFE NFCE }
enum SefazStatusType    { ONLINE UNSTABLE OFFLINE }
enum CompanyContactSource { MANUAL WHATSAPP IMPORT }
enum CompanyContactStatus { PENDING_LINK LINKED ARCHIVED }
enum ContractStatus     { ACTIVE CANCELLED SUSPENDED }
enum RemoteAgentCommandType {
  REAPPLY_ALIAS REAPPLY_CONFIG UPGRADE_CLIENT ROTATE_TOKEN_REQUIRED
}
enum RemoteAgentCommandStatus { PENDING DELIVERED ACKNOWLEDGED CANCELLED FAILED }
enum RemoteAddressBookCredentialScope { GLOBAL COMPANY }
```

---

## ConvenÃ§Ãµes do schema

- Todos os IDs sÃ£o `String` gerados com `@default(cuid())`
- `createdAt` e `updatedAt` em todos os modelos
- `deletedAt` (soft delete) em modelos onde histÃ³rico Ã© necessÃ¡rio
- Ãndices em campos de busca frequente: `cnpj`, `rustdeskId`, `agentToken`, `externalId`
- Foreign keys com `onDelete: Cascade` onde semanticamente correto (ex: mensagens deletadas com conversa)
