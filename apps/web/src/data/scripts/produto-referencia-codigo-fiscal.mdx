---
id: "SCR-004"
title: "Criar Refer?ncia de Fornecedor com C?digo Fiscal do Produto"
description: "Cria um registro em 'produto_referencia' para todos os produtos, associando-os a um participante (ex: matriz) e usando o pr?prio c?digo fiscal do produto como c?digo de refer?ncia do fornecedor. Essencial para opera??es de transfer?ncia entre matriz e filial. ATEN??O: Backup obrigat?rio. O script n?o duplica refer?ncias caso j? existam."
category: "Produtos"
author: "Analista do Sistema"
createdAt: "2025-10-15"
firebirdVersion: "5.0"
tags:
  - "produtos"
  - "referencia"
---

```sql
-- Script: Adicionar c?digo fiscal como refer?ncia de produto para um participante
-- Objetivo: Para cada produto, cria uma entrada na tabela de refer?ncia (produto_referencia)
--           associando-o a um participante espec?fico (geralmente a matriz), utilizando
--           o campo 'cd_fiscal' como o c?digo do produto no "fornecedor".
-- Recomenda??o: Execute um backup completo ANTES de rodar este script.
--
-- Par?metros:
-- :ID_PARTICIPANTE_FORNECEDOR -> UUID do participante (fornecedor, matriz, etc.) para o qual
--                                a refer?ncia ser? criada.
--                                Ex: '{6D7D332C-C88B-4C8D-B465-3A14EE07D382}'

INSERT INTO produto_referencia (
    id_produto_referencia,
    id_produto,
    id_participante_for,
    ativo,
    cd_produto_fornecedor,
    dt_ref,
    hr_ref,
    dt_alteracao,
    hr_alteracao
)
SELECT
    '{' || UUID_TO_CHAR(GEN_UUID()) || '}',
    pr.id_produto,
    :ID_PARTICIPANTE_FORNECEDOR,
    1,
    pr.cd_fiscal,
    CURRENT_DATE,
    CURRENT_TIME,
    CURRENT_DATE,
    CURRENT_TIME
FROM
    produto pr
WHERE
    NOT EXISTS (
        SELECT 1
        FROM produto_referencia pr_ex
        WHERE pr_ex.id_produto = pr.id_produto
          AND pr_ex.id_participante_for = :ID_PARTICIPANTE_FORNECEDOR -- <<< INFORME AQUI O ID DO PARTICIPANTE (MATRIZ/FILIAL)
    );

COMMIT;
```