// Re-export from @cadens/core package - single source of truth
export {
    ROLE_LABELS,
    SYSTEM_PERMISSIONS,
    ACCESS_MATRIX,
} from "@cadens/core/permissions";

export type {
    Role,
    PermissionKey,
    AccessControlMatrix,
} from "@cadens/core/permissions";
