"use client";

/**
 * @deprecated Use DeviceFilterPopover from `@/features/infrastructure/device/components/device-filter-popover` instead.
 */
export { DeviceFilterPopover as DeviceFilters } from "./device-filter-popover";
